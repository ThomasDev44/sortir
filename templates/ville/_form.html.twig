{{ form_start(form) }}
<td class="input-table" id="input-table">{{ form_widget(form.nom) }}</td>
<td class="input-table">{{ form_widget(form.codePostal) }}</td>
<td><input class="btn btn-success" type="submit" value="{{ button_label|default('Ajouter') }}"/></td>
{{ form_end(form) }}